#!/bin/bash
#PBS -N coupled_march_2013
#PBS -A NHAO0032
#PBS -l walltime=00:10:00
#PBS -o fcst.log
#PBS -j oe
#PBS -l select=1:ncpus=128:mpiprocs=40
#PBS -q main
#PBS -W umask=022

set -ax

module use -a ../modulefiles/derecho.intel
module load wam-ipe

# cd into the current directory (IPE/test)
cd $PBS_O_WORKDIR

# Execute the test (assume that it was compiled)
make test_rundir
make test_run NP=40
make test_check

#rc=$?
#[ $rc -ne 0 ] && echo "Failure code $rc" && exit
#[ ! -f IPE_State.apex.201303160005.nc ] && echo "Output file IPE_State.apex.201303160005.nc not created." && exit
